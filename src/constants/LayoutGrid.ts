import { StyleSheet } from "react-native";

export const HomeGrid = StyleSheet.create({
    container: {
        flex: 1,
        marginHorizontal: 20,
        marginTop: 70,
    },
});

export const PlantFullScreenGrid = StyleSheet.create({
    container: {
        paddingHorizontal: 20,
        paddingTop: 20,
    },
});
