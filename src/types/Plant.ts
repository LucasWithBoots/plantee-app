export type Plant = {
    id: number;
    name: string;
    location: string;
    water: string;
    image: string;
};

export type PlantNoID = {
    name: string;
    location: string;
    water: string;
    image: string;
};
